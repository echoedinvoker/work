// const finishPath = `${read('outputFolder.txt')}\\${moment(date).format(
//   'YYYY'
// )}\\${moment(date).format('MMMM')}\\finish_${moment(new Date()).format(
//   'YYMMDD'
// )}.csv`;

// write('finishPath.txt', finishPath);
